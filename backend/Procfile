#!/bin/bash
# Railway deployment script for RINOS Bikes Backend

# Railway will use this to start the application
# Make sure this matches what Railway expects

uvicorn api.main:app --host 0.0.0.0 --port $PORT
